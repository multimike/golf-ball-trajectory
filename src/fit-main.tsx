import React from "react"
import ReactDOM from "react-dom/client"
import {FitPage} from "./FitPage.tsx"
import "./fit.css"

ReactDOM.createRoot(document.getElementById("root")!).render(
	<React.StrictMode>
		<FitPage />
	</React.StrictMode>
)
